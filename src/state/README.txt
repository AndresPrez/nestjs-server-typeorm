The purpose of this directory ("state") is to store any file or logic used to keep a global state of the server.
Usually needed when servers need to store some dynamic state. There are cases where Servers do not need a global state.
